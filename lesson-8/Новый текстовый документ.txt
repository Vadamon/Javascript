1) Можно использовать функцию замыкания в функции телепортации и для функции с едой


2) Вывод будет function a(){alert(this);} так, как:
   
    В первом условии Атрибута «a» в window нет, условие не выполняется,
 поэтому undefined.

	Второй и третий примеры я не смог понять((

	В четвёртом логично, что a и arguments[2] — это ссылка на одно и то же значение.
 Иначе попадаем на память.

	В пятом: this в глобальной области указывает на window. Вызов call() у функции с
 параметром null равнозначно вызову call() без аргументов.